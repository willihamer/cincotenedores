import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

export default function AddRestaurantForm({navigation, toastRef, setIsLoading}) {

    return(
        <View>
            <Text>Add Restaurant Form...</Text>
        </View>
    )

}